import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function SigninPage() {
  return (
    <SafeAreaView>
      <Text className="text-violet-500">Sign in page</Text>
    </SafeAreaView>
  );
}
